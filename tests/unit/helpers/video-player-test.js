import { videoPlayer } from '../../../helpers/video-player';
import { module, test } from 'qunit';

module('Unit | Helper | video player');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = videoPlayer(42);
  assert.ok(result);
});
